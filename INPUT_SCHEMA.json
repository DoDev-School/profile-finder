{
  "title": "Configuração",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "hashtags": {
      "title": "Hashtags",
      "type": "array",
      "editor": "stringList",
      "description": "Lista de hashtags para procurar (sem #). Ex.: [\"modafeminina\", \"acessorios\"]",
      "items": { "type": "string" },
      "minItems": 1
    },
    "maxProfiles": {
      "title": "Máximo de perfis",
      "type": "integer",
      "editor": "number",
      "description": "Quantidade máxima de perfis a retornar.",
      "minimum": 1,
      "default": 100
    },
    "minFollowers": {
      "title": "Seguidores mínimos",
      "type": "integer",
      "editor": "number",
      "description": "Apenas perfis com pelo menos este número de seguidores serão retornados.",
      "minimum": 0,
      "default": 1000
    },
    "lookbackPosts": {
      "title": "Posts recentes analisados",
      "type": "integer",
      "editor": "number",
      "description": "Quantos posts recentes por perfil analisar para validar o uso das hashtags.",
      "minimum": 5,
      "default": 20
    },
    "minHashtagHitRatePct": {
      "title": "Taxa mínima de uso das hashtags (%)",
      "type": "integer",
      "editor": "number",
      "description": "Porcentagem de posts recentes que devem conter ao menos uma das hashtags. Ex.: 20 = 20%",
      "minimum": 0,
      "maximum": 100,
      "default": 20
    },
    "perHashtagPostSample": {
      "title": "Amostra de posts por hashtag",
      "type": "integer",
      "editor": "number",
      "description": "Para cada hashtag, quantos posts coletar como base inicial para mapear autores.",
      "minimum": 10,
      "default": 200
    },
    "useApifyProxy": {
      "title": "Usar Apify Proxy",
      "type": "boolean",
      "editor": "checkbox",
      "description": "Recomendado para scrapers do Apify Store.",
      "default": true
    },
    "proxyGroups": {
      "title": "Grupos de Proxy (opcional)",
      "type": "array",
      "editor": "stringList",
      "description": "Ex.: [\"RESIDENTIAL\"]",
      "items": { "type": "string" }
    },
    "instagramSession": {
      "title": "Cookie de sessão do Instagram (sessionid)",
      "type": "string",
      "editor": "textfield",
      "description": "Valor do sessionid do cookie (somente o valor, sem o prefixo 'sessionid=')."
    }
  },
  "required": ["hashtags"]
}
